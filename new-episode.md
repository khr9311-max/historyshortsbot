---
description: 대본을 받아 에피소드 디렉토리와 Manim 도해 씬을 생성한다
argument-hint: [EP번호]
---

# 새 에피소드 구축

대상 에피소드: $ARGUMENTS

## 절차

1. **스캐폴드 생성**
   `./scripts/new_episode.sh $ARGUMENTS` 실행.
   이미 디렉토리가 있으면 덮어쓰지 말고 중단한 뒤 사용자에게 알린다.

2. **대본 입력 요청**
   claude.ai 프로젝트에서 뽑은 대본과 씬 분해표를 붙여넣어 달라고 요청한다.
   받은 내용을 `episodes/$ARGUMENTS/script.md`에 저장한다.

3. **scenes.tsv 작성**
   씬 분해표를 파싱해 `scenes.tsv`를 만든다. 컬럼: `scene / kind / move / dur / note`
   검증할 것:
   - 1번 씬이 `ai_hero`인가
   - `diagram` 컷이 3개 연속이 아닌가
   - 컷 배분이 `ai_hero` 3 / `ai_still` 2~3 / `diagram` 4~5 범위인가
   - 총 길이가 45~55초인가
   위반이 있으면 **수정하지 말고 사용자에게 보고**한다.

4. **sources.md 작성**
   대본에 등장한 모든 수치·연도·인용을 항목으로 추출해 출처 기입란을 만든다.
   출처는 임의로 채우지 마라. 빈칸으로 두고 사용자가 채우게 한다.

5. **Manim 도해 씬 작성**
   `kind`가 `diagram`인 모든 씬에 대해 `episodes/$ARGUMENTS/diagrams/S0N.py`를 작성한다.

   반드시 지킬 것:
   - `docs/02_스타일_바이블.md`의 색상 상수(`BG`/`INK`/`ACCENT`/`SUB`/`MUTE`)를 그대로 사용
   - 도해 패턴 5종(인과 화살표 / 타임라인 / 지도 이동 / 수치 그래프 / 픽토그램 대비) 안에서만
   - 한 화면에 `ACCENT`는 한 곳만
   - 요소는 한 번에 하나씩 등장, `run_time` 최소 1.2초, 등장 후 0.8초 정지
   - 화면 동시 요소 최대 5개
   - 해상도 1080×1920 세로
   - 기존 에피소드의 같은 패턴 씬이 있으면 **먼저 읽고 그 구조를 따른다**

6. **렌더 및 보고**
   각 도해 씬을 렌더해 `assets/clips/`에 배치한다.
   완료 후 아래를 보고한다:
   - 생성된 씬 목록과 각 길이
   - 위 3번 검증에서 걸린 항목
   - `sources.md`에서 사용자가 채워야 할 항목 수

## 하지 말 것

- 출처를 추측해서 채우기
- 스타일 바이블 값을 임의로 변경하기
- 대본 내용을 임의로 수정하기 (문제가 있으면 지적만)
